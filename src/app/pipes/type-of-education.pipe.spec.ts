import { TypeOfEducationPipe } from './type-of-education.pipe';

describe('TypeOfEducationPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeOfEducationPipe();
    expect(pipe).toBeTruthy();
  });
});
