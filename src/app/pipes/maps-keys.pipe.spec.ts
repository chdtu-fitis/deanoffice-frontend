import { MapsKeysPipe } from './maps-keys.pipe';

describe('MapsKeysPipe', () => {
  it('createTeacher an instance', () => {
    const pipe = new MapsKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
